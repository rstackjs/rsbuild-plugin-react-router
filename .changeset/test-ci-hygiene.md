---
---

Test and repo hygiene only — no published code changes, no release needed:
require native TypeScript support for benchmark scripts, remove a committed
Playwright artifact, and ignore nested Playwright output directories.
