---
---

Test and repo hygiene only — no published code changes, no release needed:
skip `.mts`-spawning benchmark tests when the Node runtime lacks default
type stripping, remove a committed Playwright artifact, and ignore nested
Playwright output directories.
