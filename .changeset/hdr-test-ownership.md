---
"rsbuild-plugin-react-router": patch
---

Strengthen HDR intent regression coverage: replace the vacuous CSS-only commit test with an actual CSS ownership transition probe, keep superseded async evaluation coverage, and clarify intent-tracker revision naming without behavior changes.
